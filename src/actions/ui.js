import { types } from "../types/types";


export const openNavbar = () => ({ type: types.uiOpenNavbar })

export const closeNavbar = () => ({ type: types.uiCloseNavbar })

export const toggleNavbar = () => ({ type: types.uiToggleNavbar })


